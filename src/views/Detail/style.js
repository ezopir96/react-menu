import styled from "styled-components";

export const DetailContainer = styled.div`
  .food_img {
    width: 100%;
  }
  h1 {
    font-size: .5rem;
    text-align: center;
  }
  .waiting {
    display: none;
  }
`